<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ejercicioencasa">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity android:name=".rest" />
        <activity android:name=".UserInfo" />
        <activity android:name=".WarmUpActivity" />
        <activity android:name=".UnitExercise" />
        <activity android:name=".muscular" />
        <activity android:name=".Friendly" />
        <activity android:name=".Difficulty" />
        <activity android:name=".RoutineTimeInput" />
        <activity android:name=".GoalSelect" />
        <activity android:name=".HeightInput" />
        <activity android:name=".WeightInput" />
        <activity android:name=".GenderSelect" />
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>